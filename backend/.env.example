# Replace YOUR_PASSWORD with your PostgreSQL password (URL-encode special chars, e.g. @ -> %40).
DATABASE_URL=postgresql+asyncpg://postgres:YOUR_PASSWORD@localhost:5432/monitor
DATABASE_URL_SYNC=postgresql://postgres:YOUR_PASSWORD@localhost:5432/monitor
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=qwen3:8b

# X (Twitter) API v2 — pay-per-use; get Bearer Token at https://developer.x.com
X_BEARER_TOKEN=
# Facebook Graph API — create app at https://developers.facebook.com
FB_ACCESS_TOKEN=
FB_PAGE_IDS=
